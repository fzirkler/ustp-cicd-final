matrix:
- name: Markdown
  sources:
  - '**/*.md'
  aspell:
    lang: en # oder 'en', je nach Hauptsprache
  dictionary:
    wordlists:
    - spellignore.pws
  pipeline:
  - pyspelling.filters.markdown:
  - pyspelling.filters.html:
      comments: false 
      - code
      - pre
      - script
      - style
  - pyspelling.filters.url: